# Deferred legacy-project work

The following owner-authorized sources are preserved for later dedicated work. Their future completion is separate from Genesis abstraction and must not be counted as Genesis implementation progress.

| Legacy source | Current role | Future phase |
|---|---|---|
| `F:\Aetherius OS` | Hashed architecture/component abstractions and failure-test input | Stabilize/backup, deduplicate, define canonical build, repair tests, then finish as its own program |
| `F:\ai chat conversations` | Traceable research corpus | Continue thematic extraction as Genesis requirements demand |
| `F:\AI Digital Twin` | Memory, capability-truth, benchmark and adapter research | Audit canonical architecture, repair environment/tests, then finish as its own program |
| `F:\Downloads` | Case-by-case discovery pool | Evaluate only named candidates required by an active project |

Returning to these projects requires an explicit work phase with its own baseline, backup, dirty-state check, build contract and verification report. Genesis never modifies them implicitly.
